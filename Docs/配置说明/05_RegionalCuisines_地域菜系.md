# RegionalCuisines（地域菜系）说明

对应配置文件：`Config/RegionalCuisines.json`

## 定位

地域菜系是“区域饮食文化”的独立维度，建议单独维护，避免与分类/标签/技法等维度耦合。

## 当前结构

- `八大菜系`
- `其他地方菜系`

## 维护建议

- 条目保持“菜系名称”粒度，不与具体菜名混用。
- 若未来要支持“地域别名/同义词”（例如同一地区多种称呼），建议采用独立别名映射文件或在此文件增加映射结构。

